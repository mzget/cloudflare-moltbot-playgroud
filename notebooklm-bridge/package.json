{
  "name": "notebooklm-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "HTTP bridge that wraps the local notebooklm-mcp server and exposes it as a REST API",
  "scripts": {
    "start": "node src/bridge.js",
    "dev": "node --watch src/bridge.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "latest"
  }
}
